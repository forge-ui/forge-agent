# Forge Agent uses public npm packages and does not require package auth.
# Add app-specific secrets here when connecting a real backend.

# Optional: override the Umo editor URL.
# When empty, production loads /umo from the same site with an opaque sandbox.
# Example: https://umo.agent.example.com
NEXT_PUBLIC_UMO_ORIGIN=
